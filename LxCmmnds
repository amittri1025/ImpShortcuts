#turn on : nmcli r wifi on
#search : nmcli dev wifi
#connect : nmcli dev wifi connect "ssid" password "pswd"
#status nmcli dev status


# deleting non empty folder:
  rm-rf folder-name

# removing git from project : just remove .git folder
# web cam disable /enable
# webcam disable : sudo modprobe -r uvcvideo
#webcam; enable : sudo rmmod -f uvcvideo

